version: "1.0"
linter: jetbrains/qodana-dotnet
include:
  - name: All
exclude:
  - name: GeneratedCode
    paths:
      - "Library/**"
      - "Packages/**"
      - "ProjectSettings/**"
      - "Temp/**"
      - "Build/**"
      - "ci/**"
failThreshold: 0
